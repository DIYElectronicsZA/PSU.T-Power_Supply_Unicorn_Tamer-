// A task runner that runs a python program
{   "version": "2.0.0",
    "command": "python",
    "showOutput": "always",
    "windows": {
        "command": "python.exe"
    },
    "args": [
        "${file}"
    ],
    "problemMatchers": [
        "$tsc-watch"
    ]
}